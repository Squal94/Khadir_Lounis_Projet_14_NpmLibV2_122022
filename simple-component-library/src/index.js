import React from "react";
import ReactDOM from "react-dom/client";
import SelectItem from "./lib/SelectItem";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SelectItem />
  </React.StrictMode>
);
